import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.masmelaaa.quincena',
  appName: 'Quincena',
  webDir: 'dist-apk',
  backgroundColor: '#000000',
  android: {
    backgroundColor: '#000000',
  },
};

export default config;
